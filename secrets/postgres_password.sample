-- POSTGRES PASSWORD SAMPLE --
Replace this file with your actual secret at `./secrets/postgres_password` (gitignored).

Example:
localpassword

DO NOT commit the real `postgres_password` file. See secrets/README.md for details.